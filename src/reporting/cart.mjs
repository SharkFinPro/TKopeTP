export default class Cart {
    constructor(cart, paymentMethod, time) {
        this.cart = cart;
        this.paymentMethod = paymentMethod;
        this.time = time;
    }

    getTotalMoney() {

    }
}